// import React from 'react';

// const BudgetAlert = ({ alert }) => {
//   if (!alert) return null;

//   const isExceeded = alert.alert;

//   return (
//     <div className={`alert ${isExceeded ? 'alert-danger' : 'alert-success'} fw-semibold`} role="alert">
//       {isExceeded
//         ? `⚠ Budget Exceeded! Spent ₹${alert.total_spent} of ₹${alert.limit}`
//         : `✅ Within Budget: Spent ₹${alert.total_spent} of ₹${alert.limit}`}
//     </div>
//   );
// };

// export default BudgetAlert;
